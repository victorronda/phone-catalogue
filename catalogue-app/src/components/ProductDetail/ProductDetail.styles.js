import styled from 'styled-components';

export const FlexContainerDetail = styled.div`
    background: rgba(255,255,255,0.3);
    max-width: 60vw;
    margin: 0 auto;
    display: flex;
    flex-direction: row;
    color: white;
`;

export const ImageDetail = styled.img`
    min-width: 20vw;
    max-width: 20vw;
`;

